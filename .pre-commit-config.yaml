repos:
  - repo: local
    hooks:
      - id: pytest
        name: pytest (suite complète)
        entry: .venv/bin/python3 -m pytest tests/ -q
        language: system
        pass_filenames: false
        always_run: true
